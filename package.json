{
  "name": "teacherplan",
  "private": true,
  "workspaces": [
    "web",
    "sanity"
  ],
  "scripts": {
    "dev:web": "npm run develop --workspace web",
    "dev:sanity": "npm run dev --workspace sanity",
    "build:web": "npm run build --workspace web",
    "build:sanity": "npm run build --workspace sanity",
    "seed:sanity": "npm run seed --workspace sanity"
  }
}
